# Vanilla FastAPI (no AI tooling) — rigscore fixture

Minimal FastAPI project with no AI/agent tooling. Used by the no-ai-tooling
regression test to assert rigscore does not penalize projects that simply
aren't using AI coding assistants.
