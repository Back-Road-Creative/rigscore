version: "3.9"
services:
  webapp:
    image: example/webapp:latest
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: development
    volumes:
      - ./data:/data
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ./models:/root/.ollama
