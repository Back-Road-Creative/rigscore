chore(deps): add @vitest/coverage-v8@3.2.4 devDependency

Required peer dep for `vitest --coverage` with the v8 provider. Lands as
its own PR (with its ~864-line lockfile delta) so the follow-up Wave 6
PR — which actually enables the coverage gate in vitest.config.js and
ci.yml — stays under the 300-sum diff cap.

No behavior change on its own: `npm test` still runs without
`--coverage`. The follow-up PR adds the coverage step to CI and the
threshold to vitest.config.js.

Wave 6a of the fix plan (split from Wave 6 for diff-cap reasons).
