- The release workflow now dispatches `docker-publish` and `release-provenance` after creating the release, so the GHCR image and provenance publish on every release (a `GITHUB_TOKEN`-created tag does not re-trigger `push: tags` workflows).
