- **CI docs**: documented running rigscore in any CI — a generic exit-code contract (`0`/`1`/`2`), the `--ci` / `--sarif` / `--fail-under` flags, and a GitLab CI `.gitlab-ci.yml` example alongside the existing GitHub Action.
